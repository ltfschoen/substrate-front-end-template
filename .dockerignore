# items that do not need to be in a Docker image as they
# are not used by the build system
docker/Dockerfile*
.git
.dockerignore

.gitignore
README.md

# items that will be build during Docker image creation
node_modules
build
